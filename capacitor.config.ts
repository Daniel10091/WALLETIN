import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.walletin',
  appName: 'walletin',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
